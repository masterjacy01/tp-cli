# tp-cli-install

One-command installer shim for `tp-cli`.

## Usage

```bash
npx -y tp-cli-install install git+https://github.com/masterjacy01/tp-cli.git
```

This wrapper installs the target directly via `pipx` (preferred) or `python -m pip`.
